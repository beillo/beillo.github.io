import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://beillo.github.io',
  // se usar repositório com nome diferente de username.github.io, descomentar:
  // base: '/portfolio',
});
